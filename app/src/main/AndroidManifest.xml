<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.autoleave">
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoLeave">
        <activity android:name=".Personal_Info"></activity>
        <activity android:name=".activities.categoryMain" />
        <activity android:name=".PreviewAdd" />
        <activity android:name=".NoDriver" />
        <activity android:name=".Driver" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AddVehicleFormat" />
        <activity android:name=".LoadingStart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>